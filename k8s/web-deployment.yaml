apiVersion: apps/v1
kind: Deployment
metadata:
  name: aix-web
  namespace: aix
  labels:
    app: aix-web
spec:
  # Single writer: exactly one pod owns the SQLite file on the RWO PVC.
  replicas: 1
  # RWO volume can't be attached to two pods at once — recreate, don't roll.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: aix-web
  template:
    metadata:
      labels:
        app: aix-web
    spec:
      containers:
        - name: web
          image: ghcr.io/trevormil/aix-web:latest
          imagePullPolicy: Always
          # Runs on the Node runtime (see Dockerfile.web): better-sqlite3 replaced
          # bun:sqlite to end the Next-on-Bun RSS leak. Migrations apply at boot via
          # instrumentation.ts; the start command lives in the image CMD.
          ports:
            - containerPort: 3000
              name: http
          env:
            # SQLite path on the mounted PVC (see volumeMounts below).
            - name: AIX_DB_PATH
              value: /data/aix.db
            # Public URL — OAuth redirect_uri + email links (behind the ingress,
            # the request's own origin is the internal service host).
            - name: AIX_PUBLIC_URL
              value: https://aix.trevormil.com
          envFrom:
            - secretRef:
                name: aix-secrets
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              # Fits the whole workload on ONE node (autoscaler min=1).
              memory: 256Mi
            limits:
              cpu: "1"
              # Node+Next+better-sqlite3 steady-state is ~250-400Mi; 768Mi is a
              # safety ceiling (the leak that once needed 2Gi is gone).
              memory: 768Mi
          volumeMounts:
            - name: db
              mountPath: /data
      volumes:
        - name: db
          persistentVolumeClaim:
            claimName: aix-db
